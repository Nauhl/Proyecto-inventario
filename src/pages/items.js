
import { getAllItems, createNewItem } from "../lib/ctrlItem";
import { getAllLocations } from "../lib/ctrlLocation";
import { getAllRooms } from "../lib/ctrlRoom";
import { getAllCategories } from "../lib/ctrlCategory";
import { getAllConditions } from "../lib/ctrlCondition";

getAllItems()

export default function itemsPage() {
    return(
    <h1>Item page</h1>
        )
}